---
phase: 02-tracking-portal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/env.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - vitest.config.ts
  - package.json
  - src/test/setup-dom.ts
autonomous: true
requirements: [PORT-06]
user_setup: []

must_haves:
  truths:
    - "The accent colour is a single swappable token (--accent) consumed via Tailwind text-accent/bg-accent — never a raw hex in components"
    - "Error states can display a contact phone number and share links can be minted, with no hardcoded values or extra developer setup needed in dev/test"
    - "The app shell (layout) is a min-height flex column so pages fill a 375px viewport and centre content"
    - "Component (DOM) tests can run under vitest via a per-file jsdom environment without breaking the existing node-env test suite"
  artifacts:
    - path: "src/lib/env.ts"
      provides: "CONTACT_PHONE + SHARE_TOKEN_SECRET zod fields"
      contains: "CONTACT_PHONE"
    - path: "src/app/globals.css"
      provides: "--accent CSS variable + --color-accent theme alias"
      contains: "--accent"
    - path: "src/test/setup-dom.ts"
      provides: "@testing-library/jest-dom matchers for component tests"
      contains: "@testing-library/jest-dom"
  key_links:
    - from: "src/app/globals.css"
      to: "Tailwind theme"
      via: "@theme inline --color-accent"
      pattern: "--color-accent"
    - from: "vitest.config.ts"
      to: "src/test/setup-dom.ts"
      via: "setupFiles"
      pattern: "setup-dom"
---

<objective>
Lay the Phase 2 foundation that every other portal plan consumes: the single swappable accent theme token (D-02), the two new validated env fields the error UI and share link need (CONTACT_PHONE for D-11, SHARE_TOKEN_SECRET for D-12/PORT-08), the responsive app shell, and DOM-capable component testing so later UI plans have automated verification.

Purpose: Centralise theming + config + test infrastructure so component and wiring plans never hardcode a hex, never read process.env directly, and can assert rendered DOM. This is the shared seam — get it right once.
Output: Modified env schema, accent token in globals.css, responsive layout, jsdom test setup, updated devDependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tracking-portal/02-CONTEXT.md
@.planning/phases/02-tracking-portal/02-PATTERNS.md

<interfaces>
Existing env schema (src/lib/env.ts) — extend the `.object({...})` block BEFORE the `.refine(...)`:
```typescript
const envSchema = z.object({
  PALLEX_MOCK: z.enum(['true','false']).default('false').transform((v) => v === 'true'),
  PALLEX_BASE_URL: z.string().url(),
  // ...existing fields...
  UPSTASH_REDIS_REST_TOKEN: z.string().min(1),
}).refine(/* PALLEX credential check */);
```
The `env` export is a lazy Proxy that calls `parseEnv(process.env)` on first property access. New fields MUST have defaults (or be optional) so existing tests — which set only PALLEX_*/SUPABASE_*/UPSTASH_* — keep parsing.

Existing globals.css pattern:
```css
@import "tailwindcss";
:root { --background:#ffffff; --foreground:#171717; }
@theme inline { --color-background: var(--background); --color-foreground: var(--foreground); }
```

Existing vitest.config.ts: `environment: 'node'`, `globals: true`, alias `@` -> `./src`. Existing tracking/nexus/mocks tests rely on node env — DO NOT change the global environment to jsdom.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CONTACT_PHONE and SHARE_TOKEN_SECRET to the env schema (D-11, D-12)</name>
  <read_first>
    - src/lib/env.ts (the schema being modified — read the full file)
    - .planning/phases/02-tracking-portal/02-PATTERNS.md section "src/lib/env.ts (config — extend, D-11)"
  </read_first>
  <files>src/lib/env.ts, .env.local</files>
  <action>
    Inside the existing `z.object({ ... })` block in envSchema (after UPSTASH_REDIS_REST_TOKEN, before the closing `})` and the `.refine(...)`), add exactly two fields:
    ```typescript
    CONTACT_PHONE: z.string().min(1).default('+44 000 000 0000'),
    SHARE_TOKEN_SECRET: z.string().min(32).default('dev-only-insecure-share-secret-change-me'),
    ```
    Rationale for defaults: this app runs entirely in MSW mock mode in Phases 1-3 with no real secrets; the 40-char default keeps the `.min(32)` constraint satisfied so the lazy env Proxy parses successfully in dev and in the existing node-env unit tests (which set no portal env vars). The real values drop in via .env at deploy/Phase 4. Do NOT make SHARE_TOKEN_SECRET optional — a missing secret must never silently disable signing; the default guarantees a value of valid length.
    Also append both vars to .env.local (read it first; append, do not rewrite existing lines):
    ```
    CONTACT_PHONE=+44 000 000 0000
    SHARE_TOKEN_SECRET=dev-only-insecure-share-secret-change-me
    ```
    Do not touch the `.refine(...)` block or any other field.
  </action>
  <acceptance_criteria>
    - `grep -c "CONTACT_PHONE" src/lib/env.ts` returns >= 1
    - `grep -c "SHARE_TOKEN_SECRET" src/lib/env.ts` returns >= 1
    - `grep "z.string().min(32)" src/lib/env.ts` matches (SHARE_TOKEN_SECRET length constraint present)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/lib` passes (existing env-dependent suites still parse)
  </acceptance_criteria>
  <verify>
    <automated>cd "$PWD" && npx tsc --noEmit && npx vitest run src/lib/env 2>/dev/null || npx vitest run src/lib</automated>
  </verify>
  <done>Both env fields present with stated zod constraints and defaults; tsc clean; existing lib tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add the swappable accent theme token and responsive app shell (D-01, D-02, D-03, PORT-06)</name>
  <read_first>
    - src/app/globals.css (modify — read full file)
    - src/app/layout.tsx (modify — read full file)
    - .planning/phases/02-tracking-portal/02-PATTERNS.md sections "src/app/globals.css" and "src/app/layout.tsx"
  </read_first>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <action>
    In src/app/globals.css: add `--accent: #2563eb;` to the existing `:root {}` block (alongside --background/--foreground), and add `--color-accent: var(--accent);` to the existing `@theme inline {}` block (alongside --color-background). Add a one-line comment `/* blue-600 — single swappable brand token (D-02) */` next to --accent. Do not introduce any other colours. This makes `text-accent`, `bg-accent`, `border-accent`, `ring-accent`, `hover:bg-accent/90` available to all components — components MUST reference these utilities, never #2563eb.
    In src/app/layout.tsx: change `metadata.title` to `"Derby Aggs — Track your delivery"` and `metadata.description` to `"Track your Pall-Ex delivery — enter your tracking number and delivery postcode."`. Keep the existing font variables and the `<body className="min-h-full flex flex-col">` shell unchanged (it already lets pages fill height via flex-1). Do NOT add a logo asset — the wordmark in page.tsx (Plan 04) is the D-03 placeholder.
  </action>
  <acceptance_criteria>
    - `grep -- "--accent:" src/app/globals.css` matches
    - `grep -- "--color-accent: var(--accent)" src/app/globals.css` matches
    - `grep "Derby Aggs — Track your delivery" src/app/layout.tsx` matches
    - `grep "flex flex-col" src/app/layout.tsx` still matches (shell preserved)
    - `grep -c "2563eb" src/app/globals.css` returns exactly 1 (hex appears ONLY in the token definition)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "$PWD" && npx tsc --noEmit && test "$(grep -c 2563eb src/app/globals.css)" = "1"</automated>
  </verify>
  <done>Accent token defined once and aliased into the Tailwind theme; layout title updated; responsive flex shell intact; the only literal hex in the codebase for the accent lives in globals.css.</done>
</task>

<task type="auto">
  <name>Task 3: Enable DOM-capable component testing (jsdom + testing-library) without breaking node-env suites</name>
  <read_first>
    - vitest.config.ts (modify — read full file)
    - package.json (modify — read the devDependencies block)
    - .planning/phases/02-tracking-portal/02-PATTERNS.md section "Test file structure"
  </read_first>
  <files>package.json, vitest.config.ts, src/test/setup-dom.ts</files>
  <action>
    Install DOM test deps as devDependencies: run `npm install -D jsdom @testing-library/react @testing-library/dom @testing-library/jest-dom`. (These are additive; existing deps untouched.)
    Create src/test/setup-dom.ts with exactly:
    ```typescript
    import '@testing-library/jest-dom/vitest';
    ```
    In vitest.config.ts: keep `environment: 'node'` as the GLOBAL default (so existing tracking/nexus/mocks suites are unaffected), and add `setupFiles: ['src/test/setup-dom.ts']` to the `test` block. The jest-dom setup import is harmless under node env (it only registers matchers used by jsdom-tagged files). Component test files in Plans 03/04 will opt into jsdom per-file with a top-of-file docblock `// @vitest-environment jsdom`. Do NOT switch the global environment to jsdom.
  </action>
  <acceptance_criteria>
    - `test -f src/test/setup-dom.ts` succeeds and it contains `@testing-library/jest-dom/vitest`
    - `grep "setupFiles" vitest.config.ts` matches and references `setup-dom`
    - `grep "environment: 'node'" vitest.config.ts` still matches (global env unchanged)
    - `node -e "require('jsdom'); require('@testing-library/react')"` exits 0 (deps installed)
    - `npx vitest run` passes the full existing suite (no regressions)
  </acceptance_criteria>
  <verify>
    <automated>cd "$PWD" && node -e "require('jsdom');require('@testing-library/react')" && npx vitest run</automated>
  </verify>
  <done>jsdom + testing-library installed; setup-dom registers jest-dom matchers; global node env preserved; full existing suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env / process.env -> app | Config values (contact phone, signing secret) enter the app here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | SHARE_TOKEN_SECRET in src/lib/env.ts | mitigate | Secret only ever read server-side via the `env` Proxy (no `NEXT_PUBLIC_` prefix, so Next.js never ships it to the client bundle); default is an obvious dev placeholder, real value supplied via server env at deploy |
| T-02-02 | Spoofing | SHARE_TOKEN_SECRET weak default | accept | `.min(32)` enforces length; dev default is explicitly labelled insecure and is only reachable in mock mode (Phases 1-3); production env overrides it. No customer data is reachable via a share link in mock mode |
| T-02-03 | Tampering | CONTACT_PHONE rendered into a tel: link | mitigate | Value originates from server env (not user input); zod `.min(1)` guarantees non-empty; rendered as link text only |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` — full existing suite passes (no regressions from test-infra change)
- Accent hex `2563eb` appears exactly once in the repo CSS (single source of truth)
</verification>

<success_criteria>
Accent is a single swappable token wired into the Tailwind theme; CONTACT_PHONE and SHARE_TOKEN_SECRET are validated env fields with mock-safe defaults; the app shell is responsive; component tests can run under jsdom per-file with no impact on the existing node-env suite.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tracking-portal/02-01-SUMMARY.md`.
</output>
