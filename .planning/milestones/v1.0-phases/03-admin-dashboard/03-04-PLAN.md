---
phase: 03-admin-dashboard
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/app/dashboard/calls/page.tsx
  - src/app/dashboard/calls/[id]/page.tsx
  - src/components/admin/CallFilters.tsx
  - src/components/admin/CallHistoryTable.tsx
  - src/components/admin/CallDetail.tsx
  - src/components/admin/TranscriptView.tsx
  - src/components/admin/RecordingPlayer.tsx
  - src/components/admin/DriverCallSubLog.tsx
autonomous: true
requirements: [ADMIN-03, ADMIN-04, ADMIN-05, ADMIN-07]
user_setup: []

must_haves:
  truths:
    - "The call history list shows date/time, duration, tracking ref, outcome badge, and masked caller, filterable by date range + outcome and searchable by tracking reference, paginated, degrading to cards on narrow screens"
    - "Each row opens a detail page showing call metadata, a speaker-labelled transcript, a recording player (or unavailable state), and the linked outbound driver-call sub-log"
    - "The caller number is never shown unmasked and no raw from_number/transcript reaches a client component as PII"
    - "Every data surface (list, transcript, recording, sub-log) has a polished empty state"
  artifacts:
    - path: "src/app/dashboard/calls/page.tsx"
      provides: "Call history page reading calls-repo listCustomerCalls with searchParams filters"
    - path: "src/app/dashboard/calls/[id]/page.tsx"
      provides: "Call detail page reading getCallById + getDriverCallsForParent"
    - path: "src/components/admin/CallHistoryTable.tsx"
      provides: "table/card list of CallSummary with outcome badges"
    - path: "src/components/admin/TranscriptView.tsx"
      provides: "speaker-labelled transcript with JSON-parse + plain-text fallback"
    - path: "src/components/admin/RecordingPlayer.tsx"
      provides: "native audio player + recording-unavailable state"
  key_links:
    - from: "src/app/dashboard/calls/page.tsx"
      to: "src/lib/repositories/calls-repo.ts"
      via: "listCustomerCalls(filters from searchParams)"
      pattern: "listCustomerCalls"
    - from: "src/app/dashboard/calls/[id]/page.tsx"
      to: "src/lib/repositories/calls-repo.ts"
      via: "getCallById + getDriverCallsForParent"
      pattern: "getDriverCallsForParent"
    - from: "src/app/dashboard/calls/[id]/page.tsx"
      to: "src/lib/admin/mask.ts"
      via: "mask caller before passing to client components (Pitfall 4)"
      pattern: "maskPhone"
---

<objective>
Build the call history and call detail surfaces (ADMIN-03/04/05/07): a filterable, searchable, paginated history list that opens a per-call detail page with speaker-labelled transcript, recording playback (with unavailable state for Phase 3 stub data), and the linked outbound driver-call sub-log. All caller PII is masked server-side before reaching any client component.

Purpose: Lets staff review what the voice agent did on every call — the auditing surface that makes the seeded (and later real) call data reviewable. Runs in parallel with Plan 03 (no shared files).
Output: `/dashboard/calls` (list + filters) and `/dashboard/calls/[id]` (detail) fully working over seed data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-dashboard/03-CONTEXT.md
@.planning/phases/03-admin-dashboard/03-RESEARCH.md
@.planning/phases/03-admin-dashboard/03-PATTERNS.md
@.planning/phases/03-admin-dashboard/03-UI-SPEC.md
@.planning/phases/03-admin-dashboard/03-02-SUMMARY.md

<interfaces>
<!-- Consumed from Plan 02 -->
From src/lib/repositories/calls-repo.ts (bound default exports):
  listCustomerCalls(opts: CallListOptions): Promise<{ rows: CallSummary[]; total: number }>
  getCallById(id: string): Promise<CallRow | null>
  getDriverCallsForParent(parentCallId: string): Promise<CallRow[]>
From src/lib/admin/types.ts:
  CallSummary { id, start_at, duration_ms, tracking_ref, outcome, from_number_masked }
  CallListOptions { since?, until?, outcome?, search?, page?, pageSize? }
From src/lib/admin/mask.ts: maskPhone(raw: string | null): string
From src/types/database.ts:
  CallRow { id, from_number, outcome, tracking_ref, transcript, duration_ms, start_at, parent_call_id, ... }
  outcome: 'resolved' | 'escalated' | 'no_data' | 'failed' | null

<!-- Next.js 16: params AND searchParams are Promises — must await. Source: src/app/track/[token]/page.tsx -->
<!-- Phase 2 styling tokens (D-09): font-mono for refs/phones/timestamps, semantic outcome badge colours (green/amber/zinc), NOT accent -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Call history page + CallFilters + CallHistoryTable (ADMIN-03)</name>
  <files>src/app/dashboard/calls/page.tsx, src/components/admin/CallFilters.tsx, src/components/admin/CallHistoryTable.tsx</files>
  <read_first>
    - src/app/track/[token]/page.tsx (async Server Component + `await searchParams` Next.js 16 pattern, notFound())
    - src/components/EventHistory.tsx (list iteration + time display + Tailwind table-like layout — CallHistoryTable analog)
    - src/components/LookupForm.tsx (input styling + focus:ring-accent — CallFilters analog)
    - src/lib/repositories/calls-repo.ts (listCustomerCalls) + src/lib/admin/types.ts (CallSummary, CallListOptions) + src/lib/admin/windows.ts — Plan 02
    - .planning/phases/03-admin-dashboard/03-UI-SPEC.md §"Call History Page (ADMIN-03)" + §"Copywriting Contract > Call History" + §"Responsive Breakpoints"
    - .planning/phases/03-admin-dashboard/03-PATTERNS.md §"src/components/admin/CallHistoryTable.tsx" (outcome badge) and §"src/components/admin/CallFilters.tsx"
  </read_first>
  <action>
    1. Create `src/components/admin/CallFilters.tsx` (`'use client'`): pushes filter state to the URL (no client fetch). Two date inputs (`from`/`to`), an outcome `<select>` ("All outcomes" / Resolved / Missed / Escalated / Failed → mapping Missed→`no_data`), and a search input (placeholder "Search tracking reference…", debounced 300ms via a setTimeout ref). Use `useRouter`/`useSearchParams`/`usePathname`; a `setParam(key, value)` helper builds `URLSearchParams` (delete when empty) and `router.push`. Active-filter chips `bg-accent/10 text-accent rounded-full` with a × dismiss (aria-label "Remove filter"). Inputs styled `border-zinc-200 rounded-lg px-3 py-2 focus:ring-accent`.
    2. Create `src/components/admin/CallHistoryTable.tsx` (presentational; `'use client'` only if pagination needs local state — prefer Server-rendered with Link-based pagination): props `{ rows: CallSummary[]; total: number; page: number; pageSize: number }`. Desktop table columns Date/time (font-mono) | Duration ("1m 42s" from duration_ms) | Tracking ref (font-mono) | Outcome badge | Caller (`from_number_masked`, font-mono) | Action ("View" → `/dashboard/calls/${id}`, text-accent underline). Row hover `bg-zinc-50`. Outcome badge map (semantic, NOT accent): resolved `bg-green-100 text-green-600`, no_data(missed) `bg-amber-100 text-amber-600`, escalated/failed `bg-zinc-100 text-zinc-600`; badge has `aria-label="Outcome: …"`. Below `768px`, collapse each row to a card (`border-zinc-200 rounded-lg p-4 mb-2`) with label/value pairs. Pagination footer: "Showing X–Y of N calls" (`text-sm text-zinc-400`) + Previous/Next `<Link>`s with `?page=` (disabled at bounds). Empty states (D-02): if `total===0` and no active filters render "No calls recorded yet. Voice agent data will appear here after Phase 4."; if filters active and no match render "No calls match your filters. Try adjusting the date range or outcome." (exact UI-SPEC copy).
    3. Create `src/app/dashboard/calls/page.tsx` (async Server Component): `interface Props { searchParams: Promise<{ outcome?: string; q?: string; from?: string; to?: string; page?: string }> }`; `const sp = await searchParams`. Build `CallListOptions`: `since` from `sp.from` (parse to Date) / `until` from `sp.to`, `outcome` from `sp.outcome` (map UI label/value to CallRow outcome — Missed→`no_data`), `search` from `sp.q`, `page = Number(sp.page ?? '1')`, `pageSize = 25`. Call `listCustomerCalls(opts)`. Heading "Call history". Render `<CallFilters />` then `<CallHistoryTable rows total page pageSize />`. Compute `hasActiveFilters` to drive the correct empty state.
  </action>
  <verify>
    <automated>cd "/Users/vaughan.fawcett/Delivery assistant" && npm run typecheck && grep -q "listCustomerCalls" src/app/dashboard/calls/page.tsx && grep -q "await searchParams" src/app/dashboard/calls/page.tsx && grep -q "from_number_masked" src/components/admin/CallHistoryTable.tsx && grep -q "bg-green-100 text-green-600" src/components/admin/CallHistoryTable.tsx && grep -q "Search tracking reference" src/components/admin/CallFilters.tsx && grep -q "Voice agent data will appear here after Phase 4" src/components/admin/CallHistoryTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes.
    - calls/page.tsx awaits searchParams, builds CallListOptions, and calls `listCustomerCalls`.
    - CallHistoryTable renders `from_number_masked` (never raw), semantic (non-accent) outcome badges with aria-labels, pagination, and both empty states with exact copy.
    - CallFilters pushes date/outcome/search to the URL with a debounced search.
  </acceptance_criteria>
  <done>Staff can filter, search, and page through call history; PII is masked; empty states are polished.</done>
</task>

<task type="auto">
  <name>Task 2: Call detail page + CallDetail + DriverCallSubLog (ADMIN-03/ADMIN-07)</name>
  <files>src/app/dashboard/calls/[id]/page.tsx, src/components/admin/CallDetail.tsx, src/components/admin/DriverCallSubLog.tsx</files>
  <read_first>
    - src/app/track/[token]/page.tsx (dynamic route with `await params`, notFound())
    - src/components/EventHistory.tsx (list/table layout for the sub-log)
    - src/lib/repositories/calls-repo.ts (getCallById, getDriverCallsForParent) + src/lib/admin/mask.ts (maskPhone) — Plan 02
    - .planning/phases/03-admin-dashboard/03-UI-SPEC.md §"Call Detail Panel" + §"Copywriting Contract > Call Detail" + §"Accessibility Requirements"
    - .planning/phases/03-admin-dashboard/03-PATTERNS.md §"src/app/dashboard/calls/[id]/page.tsx" (mask before client) and §"src/components/admin/CallHistoryTable.tsx" (badge reuse)
    - .planning/phases/03-admin-dashboard/03-RESEARCH.md §"Pitfall 4" (never pass raw CallRow to client)
  </read_first>
  <action>
    1. Create `src/app/dashboard/calls/[id]/page.tsx` (async Server Component): `interface Props { params: Promise<{ id: string }> }`; `const { id } = await params`; `const call = await getCallById(id); if (!call) notFound();`; `const driverCalls = await getDriverCallsForParent(id);`. CRITICAL (Pitfall 4): mask `call.from_number` with `maskPhone()` server-side and pass ONLY the fields each child needs — never spread the raw `CallRow` into a client component. Back link "Back to call history" → `/dashboard/calls` (text-accent text-sm). Render `<CallDetail ... />`, then `<TranscriptView transcript={call.transcript} />`, `<RecordingPlayer recordingUrl={null} />` (Phase 3 has no real recordings — D-03 stub), and `<DriverCallSubLog calls={mapped} />` where mapped driver-call rows carry only `{ id, name(from transcript/derived), duration_ms, outcome }` (no caller PII; driver phone may be shown as it is managed data, but only include fields needed).
    2. Create `src/components/admin/CallDetail.tsx` (presentational, no `'use client'`): props the masked metadata `{ start_at, duration_ms, tracking_ref, outcome, callerMasked }`. 2-column grid of label (`text-sm text-zinc-500`) / value (`text-sm font-semibold text-zinc-900`); format `start_at` with date-fns, duration "Xm Ys"; reuse the same outcome-badge styling as CallHistoryTable (semantic colours).
    3. Create `src/components/admin/DriverCallSubLog.tsx` (`'use client'` — collapsible): heading "Outbound driver calls (N)" expanding on click; sub-table Driver | Phone(font-mono) | Duration | Outcome badge (same conventions). Empty state (D-02): exact UI-SPEC copy "No outbound calls were made for this call." Default collapsed; toggle has accessible button semantics.
  </action>
  <verify>
    <automated>cd "/Users/vaughan.fawcett/Delivery assistant" && npm run typecheck && grep -q "await params" "src/app/dashboard/calls/[id]/page.tsx" && grep -q "getCallById" "src/app/dashboard/calls/[id]/page.tsx" && grep -q "getDriverCallsForParent" "src/app/dashboard/calls/[id]/page.tsx" && grep -q "maskPhone" "src/app/dashboard/calls/[id]/page.tsx" && grep -q "notFound()" "src/app/dashboard/calls/[id]/page.tsx" && grep -q "Outbound driver calls" src/components/admin/DriverCallSubLog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes.
    - calls/[id]/page.tsx awaits params, calls getCallById + getDriverCallsForParent, `notFound()`s on missing call, and masks `from_number` with `maskPhone` before any client component (Pitfall 4 — no raw CallRow passed to client).
    - DriverCallSubLog renders linked driver calls with a (N) count and the exact empty-state copy.
    - CallDetail shows masked caller and reuses semantic outcome badges.
  </acceptance_criteria>
  <done>Each call opens a detail view with metadata, masked caller, and the linked driver-call sub-log.</done>
</task>

<task type="auto">
  <name>Task 3: TranscriptView (ADMIN-04) + RecordingPlayer (ADMIN-05)</name>
  <files>src/components/admin/TranscriptView.tsx, src/components/admin/RecordingPlayer.tsx</files>
  <read_first>
    - src/components/EventHistory.tsx (list iteration + time column pattern for transcript turns)
    - src/components/StatusHeader.tsx (date-fns formatting + presentational structure)
    - .planning/phases/03-admin-dashboard/03-UI-SPEC.md §"TranscriptView" + §"RecordingPlayer" + §"Copywriting Contract > Call Detail" + §"Accessibility Requirements"
    - .planning/phases/03-admin-dashboard/03-RESEARCH.md §"Open Questions" Q3 (transcript JSON vs plain text) and §"D-03" (recording stub)
  </read_first>
  <action>
    1. Create `src/components/admin/TranscriptView.tsx` (`'use client'` for scroll container): props `{ transcript: string | null }`. Section heading "Transcript". Attempt `JSON.parse(transcript)`; if it parses to an array of `{ speaker, text, ts }` turns, render each turn: speaker label (`text-sm font-semibold text-zinc-500`, "Agent"/"Customer"), timestamp (font-mono `text-zinc-400`), message (`text-sm text-zinc-900 leading-relaxed`); Customer turns get `bg-zinc-50 rounded-lg px-3 py-2`. If parse fails, render the raw string as plain text (Q3 fallback so Phase 4 plain-text transcripts still display). Scroll container `max-h-[60vh] overflow-y-auto` on desktop. Empty state (transcript null/empty): exact copy "Transcript not available for this call." (`text-sm text-zinc-500`). Speaker display prefix uses `aria-hidden` per Accessibility Requirements with logical reading order preserved.
    2. Create `src/components/admin/RecordingPlayer.tsx` (presentational; `'use client'` not required unless interactivity added): props `{ recordingUrl: string | null }`. Wrapper `border border-zinc-200 rounded-lg p-4 bg-zinc-50`; title "Call recording" (`text-sm font-semibold text-zinc-700 mb-2`). If `recordingUrl` present, render native `<audio controls src={recordingUrl} className="w-full" />` (browser-native accessibility, no custom player — UI-05/D-03). If null (Phase 3 default), render the unavailable state with exact UI-SPEC copy "Recording not yet available. Recordings are stored for 30 days after a call." (`text-sm text-zinc-500`). The 30-day retention expectation is encoded only in this copy for now (real enforcement is Phase 4).
  </action>
  <verify>
    <automated>cd "/Users/vaughan.fawcett/Delivery assistant" && npm run typecheck && grep -q "JSON.parse" src/components/admin/TranscriptView.tsx && grep -q "Transcript not available for this call" src/components/admin/TranscriptView.tsx && grep -q "<audio controls" src/components/admin/RecordingPlayer.tsx && grep -q "Recording not yet available" src/components/admin/RecordingPlayer.tsx && grep -q "30 days" src/components/admin/RecordingPlayer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes.
    - TranscriptView JSON-parses structured transcripts (speaker + timestamp + text), falls back to plain text on parse failure, and shows the exact empty-state copy.
    - RecordingPlayer uses native `<audio controls>` when a URL is present and the exact 30-day unavailable copy when null.
  </acceptance_criteria>
  <done>Transcript renders with speaker labels (structured or plain) and the recording surface shows a player or the designed unavailable state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (call detail) → client components | Raw CallRow contains from_number + transcript PII; only masked/needed fields may cross |
| browser → /dashboard/calls* | Gated by Plan 01 middleware; pages read PII-bearing rows server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Info Disclosure | caller from_number leaking to client in history/detail | mitigate | History uses `CallSummary.from_number_masked` (repo-masked, Plan 02); detail page masks `from_number` with `maskPhone()` server-side before passing to any client component — raw `CallRow` never spread to client (Pitfall 4) |
| T-03-16 | Info Disclosure | transcript PII passed wholesale to client | mitigate | `TranscriptView` receives only the `transcript` string for the specific call being viewed (already gated by auth + intentional staff action); no bulk transcript exposure; page passes scalar fields, not the whole row |
| T-03-17 | Tampering | malicious `id` / searchParams injection | mitigate | `getCallById` uses parameterised supabase `.eq('id', id)`; missing/invalid id → `notFound()`; searchParams coerced to typed CallListOptions with bounded page size |
| T-03-18 | Info Disclosure | unauthenticated access to call detail | mitigate | Route under `/dashboard/*` — gated by Plan 01 middleware (T-03-02) |
</threat_model>

<verification>
- `npm run typecheck` passes across all new files.
- Manual (informational): seeded calls list renders with masked callers and outcome badges; filters/search/pagination work; a row opens detail with transcript, recording-unavailable state, and driver sub-log.
</verification>

<success_criteria>
- Call history is filterable (date/outcome), searchable (tracking ref), paginated, and mobile-degrading.
- Call detail shows metadata, speaker-labelled transcript, recording player/unavailable state, and linked driver sub-log.
- Caller PII is masked everywhere; raw CallRow never reaches a client component.
- Every surface has a polished empty state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-dashboard/03-04-SUMMARY.md`.
</output>
