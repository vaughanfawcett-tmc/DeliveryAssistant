---
phase: 04-voice-agent-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/env.ts
  - supabase/migrations/0002_voice_recording_url.sql
  - src/types/database.ts
  - src/lib/repositories/calls-repo.ts
  - src/lib/repositories/drivers-repo.ts
  - src/test/setup-dom.ts
  - src/lib/repositories/calls-repo.write.test.ts
  - src/lib/repositories/drivers-repo.getbyid.test.ts
  - src/lib/env.voice.test.ts
autonomous: true
requirements: [DRIV-02, DRIV-04, ADMIN-05]
must_haves:
  truths:
    - "Voice/telephony env vars are validated at boot and required when PALLEX_MOCK=false"
    - "A customer call and a linked driver call can be persisted to the calls table"
    - "A driver's E.164 phone number can be fetched by id for outbound calling"
    - "The calls table has a recording_url column the dashboard can read"
  artifacts:
    - path: "supabase/migrations/0002_voice_recording_url.sql"
      provides: "recording_url column add"
      contains: "alter table calls add column"
    - path: "src/lib/repositories/calls-repo.ts"
      provides: "insertCall + updateCall write methods"
      contains: "insertCall"
    - path: "src/lib/repositories/drivers-repo.ts"
      provides: "getDriverById read method"
      contains: "getDriverById"
  key_links:
    - from: "src/lib/env.ts"
      to: "process.env voice vars"
      via: "zod schema + mock-aware refine"
      pattern: "VOICE_WEBHOOK_SECRET"
    - from: "src/lib/repositories/calls-repo.ts"
      to: "supabase calls table"
      via: "insert/update"
      pattern: "insert\\(|update\\("
---

<objective>
Add the shared persistence + configuration foundation every other Phase 4 plan
builds on: voice/telephony env vars, the `recording_url` migration + row type,
calls-repo write methods (`insertCall`/`updateCall`), and drivers-repo
`getDriverById`. No external services — pure schema/type/repo/env work, fully
unit-tested with injected fake Supabase clients (the established repo pattern).

Purpose: Wave 2/3 plans (webhooks, driver escalation, agent config) all depend on
these contracts. Defining them first prevents a scavenger hunt and lets later
plans run in parallel.
Output: Updated env schema, migration 0002, database types, two repos with new
methods, env test stubs, and unit tests for all new methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-voice-agent-production/04-CONTEXT.md

<interfaces>
<!-- Extracted from codebase — use directly, no exploration needed. -->

From src/lib/env.ts (zod schema, lazy Proxy, mock-aware refine):
- `optionalCredential` = z.preprocess(empty->undefined, z.string().min(1).optional())
- Existing required-when-live pattern lives in `.refine(...)` checking `!data.PALLEX_MOCK`
- `parseEnv(source)` exported for direct testing; `env` is a lazy Proxy over parseEnv(process.env)
- Existing no-default fail-loud examples: DASHBOARD_PASSWORD z.string().min(8), DASHBOARD_SESSION_SECRET z.string().min(32)

From src/types/database.ts:
```typescript
export interface CallRow {
  id: string; platform_call_id: string; from_number: string | null;
  direction: 'inbound' | 'outbound'; call_type: 'customer' | 'driver';
  start_at: string; end_at: string | null; duration_ms: number | null;
  outcome: 'resolved' | 'escalated' | 'no_data' | 'failed' | null;
  tracking_ref: string | null; transcript: string | null;
  disconnection_reason: string | null; parent_call_id: string | null; created_at: string;
}
export interface DriverRow { id: string; name: string; phone_e164: string; active: boolean; created_at: string; updated_at: string; }
// calls Insert = Omit<CallRow,'id'|'created_at'> & { id?: string; created_at?: string }
// calls Update = Partial<Omit<CallRow,'id'>>
```

From src/lib/repositories/calls-repo.ts:
- `createCallsRepo(client: SupabaseLike)` factory returns read methods today
- `SupabaseLike.from(table).insert(row)` -> Promise<{ data, error }>
- `SupabaseLike.from(table).update(patch).eq(col,val)` -> Promise<{ data, error }>
- Lazy `getDefaultRepo()` dynamically imports `../supabase`; each method re-exported as a free function

From src/lib/repositories/drivers-repo.ts:
- `insertDriver` pattern: `const { error } = await client.from('drivers').insert(input); if (error) throw new Error(...)`
- `SelectBuilder.eq(col,val)` then awaitable; reads warn+return, mutations throw (T-03-09)
- `getDefaultRepo()` lazy dynamic import of `../supabase`

From src/test/setup-dom.ts:
- Sets `process.env.DASHBOARD_PASSWORD ||=` and `DASHBOARD_SESSION_SECRET ||=` test values so any test touching env.ts passes the schema.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add voice/telephony env vars + recording_url migration + database type</name>
  <read_first>
    - src/lib/env.ts (the file being modified — the schema + refine + parseEnv)
    - supabase/migrations/0001_init_foundation.sql (migration style analog)
    - src/types/database.ts (CallRow analog being extended)
    - src/test/setup-dom.ts (env stub convention being extended)
  </read_first>
  <action>
    In src/lib/env.ts, add these keys to `envSchema`:
    - `ELEVENLABS_API_KEY: optionalCredential`
    - `ELEVENLABS_AGENT_ID: optionalCredential`
    - `ELEVENLABS_WEBHOOK_SECRET: optionalCredential`
    - `TWILIO_ACCOUNT_SID: optionalCredential`
    - `TWILIO_AUTH_TOKEN: optionalCredential`
    - `TWILIO_PHONE_NUMBER: optionalCredential`
    - `VOICE_WEBHOOK_SECRET: z.string().min(32).default('dev-only-insecure-voice-webhook-secret-change-me')`
      (mirrors SHARE_TOKEN_SECRET — never optional, so HMAC verification is never silently disabled)
    - `DRIVER_CALL_MAX_DURATION_S: z.coerce.number().int().positive().default(180)`
    - `DRIVER_CALL_MAX_RETRIES: z.coerce.number().int().nonnegative().default(2)`
    Extend the existing `.refine(...)`: when `!data.PALLEX_MOCK`, ALSO require
    ELEVENLABS_API_KEY, ELEVENLABS_AGENT_ID, ELEVENLABS_WEBHOOK_SECRET,
    TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_PHONE_NUMBER to be non-empty
    strings (same `typeof === 'string' && .length > 0` style). Update the refine
    message to name the missing-when-live voice vars and keep `path: ['PALLEX_USERNAME']`
    OR add a second refine with `path: ['ELEVENLABS_API_KEY']` (prefer a second refine
    so the two failure domains report distinctly).

    Create supabase/migrations/0002_voice_recording_url.sql following the 0001 header-comment
    style. Body: `alter table calls add column recording_url text;` plus a one-line comment
    noting D-08 (provider recording URL set on call_ended; null in mock; 30-day retention).

    In src/types/database.ts: add `recording_url: string | null;` to `CallRow` (place it
    after `transcript`). The Insert/Update mapped types derive from CallRow so they update
    automatically — no further change needed there.

    In src/test/setup-dom.ts: add `process.env.VOICE_WEBHOOK_SECRET ||= 'test-voice-webhook-secret-32chars-minimum-xx';`
    so any test whose module graph touches env.ts still satisfies the schema.
  </action>
  <verify>
    <automated>cd "$PWD" && npx tsc --noEmit && grep -q "VOICE_WEBHOOK_SECRET" src/lib/env.ts && grep -q "recording_url" src/types/database.ts && grep -q "add column recording_url" supabase/migrations/0002_voice_recording_url.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ELEVENLABS_\|TWILIO_\|VOICE_WEBHOOK_SECRET\|DRIVER_CALL_MAX" src/lib/env.ts` returns >= 9
    - `grep "recording_url" src/types/database.ts` matches `recording_url: string | null;`
    - `supabase/migrations/0002_voice_recording_url.sql` contains `alter table calls add column recording_url text`
    - `grep "VOICE_WEBHOOK_SECRET" src/test/setup-dom.ts` matches
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Voice env vars validated (required when live, mock-tolerant); recording_url exists in migration + CallRow; test env stub updated; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: calls-repo insertCall/updateCall + drivers-repo getDriverById (TDD)</name>
  <read_first>
    - src/lib/repositories/calls-repo.ts (file being modified — SupabaseLike, factory, lazy default, free-function exports)
    - src/lib/repositories/drivers-repo.ts (insertDriver throw-on-error analog; getDefaultRepo pattern)
    - src/types/database.ts (CallRow Insert/Update types after Task 1)
  </read_first>
  <behavior>
    insertCall:
    - Given a valid CallInsert, calls client.from('calls').insert(row); resolves void on { error: null }
    - On { error: { message } }, throws Error containing "[calls-repo] insert failed" and the message
    updateCall:
    - Given (platformCallId, patch), calls client.from('calls').update(patch).eq('platform_call_id', platformCallId)
    - Resolves void on success; throws Error containing "[calls-repo] update failed" on error
    getDriverById:
    - Given an id, calls client.from('drivers').select('*').eq('id', id).single()
    - Returns the DriverRow on success; returns null (never throws) when not found / error
  </behavior>
  <action>
    Extend the SupabaseLike/FromBuilder types in calls-repo.ts: `insert` already exists;
    confirm `update(patch).eq(col,val)` returns a Promise (UpdateBuilder already does).
    Add to `createCallsRepo`:
    - `async function insertCall(row: Database['public']['Tables']['calls']['Insert']): Promise<void>`
      using `const { error } = await client.from('calls').insert(row); if (error) throw new Error(\`[calls-repo] insert failed: ${error.message}\`)` (mirror insertDriver).
    - `async function updateCall(platformCallId: string, patch: Database['public']['Tables']['calls']['Update']): Promise<void>`
      using `const { error } = await client.from('calls').update(patch).eq('platform_call_id', platformCallId); if (error) throw new Error(\`[calls-repo] update failed: ${error.message}\`)`.
    Import the Database type (`import type { CallRow, Database } from '../../types/database'`).
    Return both from the factory and add free-function re-exports via getDefaultRepo (mirror getMetrics).

    In drivers-repo.ts: extend SelectBuilder to add `single(): Promise<{ data: DriverRow | null; error: { message: string } | null }>`.
    Add `async function getDriverById(id: string): Promise<DriverRow | null>` that does
    `const { data, error } = await client.from('drivers').select('*').eq('id', id).single(); if (error || !data) return null; return data as DriverRow;`
    Return from factory + add free-function re-export via getDefaultRepo.

    RED first: create src/lib/repositories/calls-repo.write.test.ts and
    src/lib/repositories/drivers-repo.getbyid.test.ts with fake injected clients
    (build minimal `SupabaseLike` fakes returning the success/error shapes per <behavior>),
    asserting all cases. Run — must fail. Then implement — must pass.
  </action>
  <verify>
    <automated>cd "$PWD" && npx vitest run src/lib/repositories/calls-repo.write.test.ts src/lib/repositories/drivers-repo.getbyid.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "insertCall\|updateCall" src/lib/repositories/calls-repo.ts` >= 4 (factory + export)
    - `grep "getDriverById" src/lib/repositories/drivers-repo.ts` matches in factory and export
    - calls-repo.write.test.ts asserts both the success (void) and throw paths for insertCall AND updateCall
    - drivers-repo.getbyid.test.ts asserts found -> DriverRow and not-found/error -> null
    - `npx vitest run` for both files: all tests pass
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>insertCall/updateCall throw-on-error and getDriverById return-null-on-miss implemented and unit-tested against fake clients; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env → app | Misconfigured/absent secrets must fail loud, never silently disable security |
| app → Supabase calls/drivers tables | Writes of PII (caller/driver numbers, transcripts, recording URLs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | VOICE_WEBHOOK_SECRET env | mitigate | `.min(32)` non-optional default mirrors SHARE_TOKEN_SECRET so HMAC verification can never be silently disabled by an empty value |
| T-04-02 | Information disclosure | recording_url column | accept | Column is nullable text on a service-role-only, RLS-enabled table; URL access control is the provider's signed-URL concern, handled in Plan 06 retention/agent-config |
| T-04-03 | Denial of service | DRIVER_CALL_MAX_* env | mitigate | Positive-int coercion with sane defaults (180s / 2 retries) gives the Plan 05 escalation machine hard runaway limits even if unset |
| T-04-04 | Information disclosure | repo write methods | mitigate | insertCall/updateCall persist only typed CallInsert fields; from_number masking remains enforced at the read boundary (existing maskPhone) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/lib/repositories src/lib/env.voice.test.ts` (if added) — all green
- `grep "recording_url" src/types/database.ts supabase/migrations/0002_voice_recording_url.sql` both match
</verification>

<success_criteria>
Voice/telephony config validated and mock-tolerant; recording_url migration + type added; calls-repo can persist customer and driver calls; drivers-repo can fetch a driver by id; all new methods unit-tested; tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-voice-agent-production/04-01-SUMMARY.md`
</output>
