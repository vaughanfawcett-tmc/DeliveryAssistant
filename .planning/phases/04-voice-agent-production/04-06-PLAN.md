---
phase: 04-voice-agent-production
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 04, 05]
files_modified:
  - src/lib/voice/agent-config.ts
  - src/lib/voice/agent-config.test.ts
  - src/lib/voice/retention.ts
  - src/lib/voice/retention.test.ts
  - src/app/dashboard/calls/[id]/page.tsx
  - .planning/phases/04-voice-agent-production/04-PRODUCTION-RUNBOOK.md
autonomous: false
requirements: [VOICE-01, VOICE-02, VOICE-03, VOICE-05, VOICE-06, ADMIN-05]
must_haves:
  truths:
    - "The ElevenLabs agent definition (prompt, tools, first message) lives in-repo as reviewable config"
    - "The agent's first message contains the AI-disclosure + recording-consent copy"
    - "The agent config registers exactly the five backend tools (lookup/request_human/contact_driver/call_started/call_ended)"
    - "A retention helper computes a 30-day cutoff for recordings/transcripts"
    - "The call detail page renders the real recording_url from the call row"
    - "A production runbook lists every external/human-only step (SC-5, SC-6, provisioning)"
  artifacts:
    - path: "src/lib/voice/agent-config.ts"
      provides: "Version-controlled agent definition + tool manifest"
      contains: "first_message"
    - path: "src/lib/voice/retention.ts"
      provides: "30-day retention cutoff helper"
      contains: "RETENTION_DAYS"
    - path: ".planning/phases/04-voice-agent-production/04-PRODUCTION-RUNBOOK.md"
      provides: "Human-action cutover runbook"
      contains: "PALLEX_MOCK"
  key_links:
    - from: "src/lib/voice/agent-config.ts"
      to: "src/lib/voice/compliance.ts DISCLOSURE"
      via: "first_message reuses the disclosure copy asserted in tests"
      pattern: "DISCLOSURE|disclosure"
    - from: "src/app/dashboard/calls/[id]/page.tsx"
      to: "call.recording_url"
      via: "RecordingPlayer recordingUrl prop"
      pattern: "recording_url"
---

<objective>
Close the phase: version-controlled ElevenLabs agent definition (prompt/tools/first message)
as in-repo config (D-01, reviewable + re-deployable), wire the dashboard's RecordingPlayer
to the real `recording_url`, add a documented 30-day retention helper (D-12), and author the
`04-PRODUCTION-RUNBOOK.md` enumerating every external/human-only step (provisioning, SC-5 noise
go/no-go, SC-6 live cutover/DPAs/Pro upgrades). These are the items that cannot be verified from
code (D-13) — captured as the human-action surface — plus the last code wiring.

Purpose: Ties the tested software to its deployment shape and makes the human cutover explicit
and complete, so SC-5/SC-6 are satisfied by a documented runbook step rather than faked code.
Output: agent config + retention helper + recording wire + runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-voice-agent-production/04-CONTEXT.md
@.planning/phases/04-voice-agent-production/04-02-SUMMARY.md
@.planning/phases/04-voice-agent-production/04-04-SUMMARY.md
@.planning/phases/04-voice-agent-production/04-05-SUMMARY.md

<interfaces>
<!-- Consume directly. -->

From Plan 02 — src/lib/voice/compliance.ts: `export const DISCLOSURE` (AI + recording copy),
used verbatim as the agent's first_message.
From Plan 04/05 — backend tool endpoints the agent config must reference:
  /api/voice/lookup_consignment, /api/voice/request_human, /api/voice/contact_driver,
  /api/voice/call_started, /api/voice/call_ended
From src/app/dashboard/calls/[id]/page.tsx (CURRENT — recording is hardcoded null):
  `const call = await getCallById(id);` (CallRow now includes recording_url after Plan 01)
  `<RecordingPlayer recordingUrl={null} />` -> change to `recordingUrl={call.recording_url}`
RecordingPlayer already handles a real URL (native audio) and the null/unavailable copy.
Env (Plan 01): DRIVER_CALL_*; retention is a documented helper (activation = runbook/ops, D-12).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agent config-as-code + retention helper</name>
  <read_first>
    - src/lib/voice/compliance.ts (DISCLOSURE — first_message source)
    - src/lib/voice/conversation-machine.ts (Plan 02 — capture/confirm/escalation behaviours the prompt must describe)
    - src/lib/voice/nato.ts (NATO read-back referenced by the prompt)
  </read_first>
  <action>
    Create src/lib/voice/agent-config.ts exporting a typed `agentConfig` object:
    `{ first_message: string; system_prompt: string; tools: VoiceToolDef[]; dtmf: { terminator: '#' } }`
    where first_message === DISCLOSURE (import it — do NOT duplicate the string), `tools` is an
    array of `{ name, url_path, description }` covering exactly the five endpoints
    (lookup_consignment, request_human, contact_driver, call_started, call_ended), and `system_prompt`
    encodes the capture+NATO-readback+confirm rule (VOICE-02), DTMF '#' fallback (VOICE-03), 3-attempt
    cap + on-demand "0"/"agent" handoff (VOICE-05/06), and the never-invent-data rule (VOICE-08).
    Export the `VoiceToolDef` type and a `TOOL_PATHS` const array for grep/test.

    Create src/lib/voice/retention.ts exporting `export const RETENTION_DAYS = 30;` and
    `retentionCutoff(now: Date = new Date()): Date` returning now minus RETENTION_DAYS, plus
    `isExpired(recordedAt: string, now?: Date): boolean`. Pure, no I/O (activation of the purge
    job is a runbook/ops step per D-12).

    Tests: agent-config.test.ts asserts first_message === DISCLOSURE, tools has exactly 5 entries
    with the 5 expected url_paths, and system_prompt mentions NATO + a "never invent" / "only state
    information returned" phrase. retention.test.ts asserts RETENTION_DAYS === 30, the cutoff math, and
    isExpired boundaries (29 days -> false, 31 days -> true).
  </action>
  <verify>
    <automated>cd "$PWD" && npx vitest run src/lib/voice/agent-config.test.ts src/lib/voice/retention.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "first_message" src/lib/voice/agent-config.ts` matches and the test proves first_message === DISCLOSURE
    - `TOOL_PATHS` contains exactly the 5 endpoint paths; test asserts length 5 + each path
    - `grep "RETENTION_DAYS = 30" src/lib/voice/retention.ts` matches
    - retention.test.ts asserts isExpired at the 30-day boundary
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Reviewable in-repo agent definition (disclosure-first, 5 tools, capture/escalation/never-invent rules) + documented 30-day retention helper, both unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire real recording_url into the call detail page</name>
  <read_first>
    - src/app/dashboard/calls/[id]/page.tsx (the file being modified — RecordingPlayer currently null)
    - src/components/admin/RecordingPlayer.tsx (already renders a real URL)
    - src/types/database.ts (CallRow.recording_url after Plan 01)
  </read_first>
  <action>
    In src/app/dashboard/calls/[id]/page.tsx, change the RecordingPlayer prop from
    `recordingUrl={null}` to `recordingUrl={call.recording_url}`. Replace the stale
    "Phase 3 stub: no recordings yet" comment with a one-line note referencing D-08
    (recording_url set on call_ended; null in mock mode). No other change — masking and the
    rest of the page are untouched.
  </action>
  <verify>
    <automated>cd "$PWD" && grep -q "recordingUrl={call.recording_url}" "src/app/dashboard/calls/[id]/page.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "recordingUrl={call.recording_url}" src/app/dashboard/calls/[id]/page.tsx` matches
    - No remaining `recordingUrl={null}` in that file
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The dashboard call-detail page plays the real recording when present and shows the unavailable copy otherwise (ADMIN-05 fed by Phase 4).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Author + review the production runbook (human-action checklist)</name>
  <action>Create .planning/phases/04-voice-agent-production/04-PRODUCTION-RUNBOOK.md as an ordered HUMAN-ACTION checklist containing the five external/human-only steps described in what-built (provision ElevenLabs Agent + Twilio UK number; set webhook URLs to /api/voice/* + signing secrets VOICE_WEBHOOK_SECRET/ELEVENLABS_WEBHOOK_SECRET/TWILIO_*; SC-5 noise go/no-go with Retell pivot note pointing at src/lib/voice/telephony/elevenlabs-twilio-adapter.ts; SC-6 Pall-Ex canary then flip PALLEX_MOCK=false; sign DPAs, activate the 30-day purge job referencing src/lib/voice/retention.ts, upgrade Vercel Pro + Supabase Pro). Each item names the exact env vars, dashboard locations, and files. State plainly that SC-5 and SC-6 are human-action gates, not code.</action>
  <what-built>
    Authored .planning/phases/04-voice-agent-production/04-PRODUCTION-RUNBOOK.md — the complete
    human-action checklist for the items that cannot be built or verified from code (D-13).
    It MUST contain, as an ordered checklist with exact commands/locations:
    1. Provision ElevenLabs Agent (upload agentConfig from src/lib/voice/agent-config.ts) + buy a Twilio UK number.
    2. Set webhook URLs to /api/voice/* and the signing secrets (VOICE_WEBHOOK_SECRET, ELEVENLABS_WEBHOOK_SECRET, TWILIO_*).
    3. Run the SC-5 real-world noise STT go/no-go (lorry cab / yard); record PASS -> stay on ElevenLabs, FAIL -> pivot to Retell (swap the telephony adapter only — note the adapter file).
    4. SC-6 live cutover: obtain Pall-Ex live credentials, run the canary, then flip PALLEX_MOCK=false.
    5. Sign DPAs; activate the 30-day retention purge job (reference src/lib/voice/retention.ts RETENTION_DAYS); upgrade Vercel Pro + Supabase Pro.
    Each item marked HUMAN-ACTION with the env var names / dashboard locations / file references involved.
  </what-built>
  <how-to-verify>
    1. Open .planning/phases/04-voice-agent-production/04-PRODUCTION-RUNBOOK.md
    2. Confirm all five external/human steps above are present, ordered, and each names the exact env vars / files / dashboards.
    3. Confirm it explicitly states the noise go/no-go (SC-5) and live cutover (SC-6) are human-action gates, not code.
    4. Confirm `grep -q "PALLEX_MOCK" 04-PRODUCTION-RUNBOOK.md` succeeds (the cutover flip is documented).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what is missing/unclear in the runbook.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-repo agent config → ElevenLabs platform | Prompt/tool manifest uploaded by a human; defines what the agent may do |
| recording_url → dashboard audio element | Provider URL rendered to staff browsers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-24 | Tampering | agent system_prompt | mitigate | The never-invent-data rule is encoded in both the prompt AND structurally in the backend (Plan 02/04); the prompt is the soft layer, the thin tool is the hard guarantee |
| T-04-25 | Information disclosure | recording_url | accept | Page is behind the iron-session admin gate (Phase 3 middleware); URL is provider-signed + short-lived; staff-only audience |
| T-04-26 | Repudiation/compliance | retention helper | mitigate | RETENTION_DAYS=30 documented + helper provided; purge-job activation is a runbook HUMAN-ACTION (D-12) — flagged, not silently skipped |
| T-04-27 | Spoofing | agent first_message | mitigate | first_message === DISCLOSURE (test-asserted) guarantees AI + recording disclosure precedes any data capture (VOICE-01) wherever the config is deployed |
</threat_model>

<verification>
- `npx vitest run src/lib/voice/agent-config.test.ts src/lib/voice/retention.test.ts` — all green
- `npx tsc --noEmit` exits 0
- `grep -q "recordingUrl={call.recording_url}" "src/app/dashboard/calls/[id]/page.tsx"`
- `grep -q "PALLEX_MOCK" .planning/phases/04-voice-agent-production/04-PRODUCTION-RUNBOOK.md`
</verification>

<success_criteria>
Reviewable in-repo agent definition (disclosure-first, 5 tools, capture/escalation/never-invent), a
documented 30-day retention helper, the dashboard wired to real recordings, and a complete production
runbook covering every human-only step (provisioning, SC-5 noise go/no-go, SC-6 live cutover, DPAs, Pro
upgrades) — so the non-code success criteria are satisfied by explicit runbook steps, never faked.
</success_criteria>

<output>
After completion, create `.planning/phases/04-voice-agent-production/04-06-SUMMARY.md`
</output>
