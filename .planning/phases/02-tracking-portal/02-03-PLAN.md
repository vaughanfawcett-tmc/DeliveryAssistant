---
phase: 02-tracking-portal
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/components/MilestoneStepper.tsx
  - src/components/MilestoneStepper.test.tsx
  - src/components/EventHistory.tsx
  - src/components/VehicleDetails.tsx
  - src/components/StatusHeader.tsx
  - src/components/TimeWindow.tsx
  - src/components/ErrorState.tsx
  - src/components/ErrorState.test.tsx
  - src/components/TrackingResult.tsx
autonomous: true
requirements: [PORT-02, PORT-03, PORT-04, PORT-05, PORT-07]
user_setup: []

must_haves:
  truths:
    - "A customer sees current status, plain-language description and ETA, then a 5-step milestone stepper, above the fold at 375px (PORT-02, D-07)"
    - "When out for delivery, the delivery time window is shown prominently (PORT-03, D-08)"
    - "The full scan/event history renders in reverse-chronological order below the timeline (PORT-04, D-09)"
    - "Vehicle reg + route status render only when the API provides a delivery route detail (PORT-07)"
    - "Each LookupFailureReason renders a distinct, helpful message; not_found and api_error show a Call us link + Try again; multiple_matches renders a safe candidate chooser (PORT-05, D-10, D-11)"
    - "A null ETA renders 'Date not yet confirmed' — never a fabricated date (PITFALLS.md Pitfall 3)"
  artifacts:
    - path: "src/components/TrackingResult.tsx"
      provides: "Result composition driven by MappedConsignment"
      contains: "MappedConsignment"
    - path: "src/components/MilestoneStepper.tsx"
      provides: "Horizontal 5-step stepper off MILESTONE_ORDER"
      contains: "MILESTONE_ORDER"
    - path: "src/components/ErrorState.tsx"
      provides: "Exhaustive Record<LookupFailureReason,...> message map + chooser + Call us"
      contains: "Record<LookupFailureReason"
  key_links:
    - from: "src/components/ErrorState.tsx"
      to: "src/lib/env.ts (CONTACT_PHONE)"
      via: "tel: link"
      pattern: "CONTACT_PHONE"
    - from: "src/components/MilestoneStepper.tsx"
      to: "src/types/tracking.ts (MILESTONE_ORDER)"
      via: "indexOf(currentStage)"
      pattern: "MILESTONE_ORDER.indexOf"
    - from: "src/components/TimeWindow.tsx"
      to: "currentStage === 'out_for_delivery'"
      via: "conditional render in TrackingResult"
      pattern: "out_for_delivery"
---

<objective>
Build every result-display and error component, driven directly by the Phase 1 type contracts (MappedConsignment, MilestoneStage/MILESTONE_ORDER, LookupFailureReason, MatchCandidate, NexusRouteDetail). These are presentation-only — no data fetching here; Plan 04 wires them to the server action. Covers the visible substance of PORT-02/03/04/05/07 and D-07..D-11.

Purpose: Keep all rendering logic in self-contained, type-driven, individually testable components so the wiring plan is trivial and the goal-backward truths (status above fold, time window, reverse-chron history, vehicle detail, distinct errors) each map to a concrete file.
Output: 9 component files (7 presentational + 2 with DOM tests for the logic-bearing ones).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tracking-portal/02-PATTERNS.md

<interfaces>
MappedConsignment (src/types/tracking.ts):
```typescript
interface MappedConsignment {
  consignmentNumber: string; plainStatus: string; description: string;
  currentStage: MilestoneStage; estimatedDelDate: string | null;
  startWindow: string | null; endWindow: string | null;
  routeDetails: NexusRouteDetail[];
}
```
MilestoneStage / MILESTONE_ORDER (src/types/tracking.ts): order is `['booked','at_hub','in_transit','out_for_delivery','delivered']`.
LookupFailureReason (src/types/tracking.ts): `'not_found' | 'postcode_mismatch' | 'multiple_matches' | 'api_error'`.
MatchCandidate (src/types/tracking.ts, added by Plan 02): `{ consignmentNumber: string; delAddressTown: string | null; plainStatus: string }`.
NexusRouteDetail (src/types/consignment.ts): `{ type: string; routeDate: string; regNo: string; round: string; status: string; palletCount: number }`.
env.CONTACT_PHONE (added by Plan 01) read via `import { env } from '@/lib/env'`.
Accent: use `text-accent`/`bg-accent`/`border-accent`/`ring-accent`/`hover:bg-accent/90` (token from Plan 01) — NEVER a raw hex.
DOM tests: top-of-file `// @vitest-environment jsdom`, then `render` from `@testing-library/react`, matchers from the Plan-01 setup file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Above-the-fold status + stepper + time window (StatusHeader, MilestoneStepper, TimeWindow) — PORT-02, PORT-03, D-07, D-08</name>
  <read_first>
    - .planning/phases/02-tracking-portal/02-PATTERNS.md sections "MilestoneStepper.tsx", "TrackingResult.tsx", "src/app/globals.css" (accent token)
    - src/types/tracking.ts (MappedConsignment, MILESTONE_ORDER, MilestoneStage)
  </read_first>
  <files>src/components/StatusHeader.tsx, src/components/MilestoneStepper.tsx, src/components/MilestoneStepper.test.tsx, src/components/TimeWindow.tsx</files>
  <action>
    All three are Server Components (no "use client"), named exports, in src/components/.
    StatusHeader({ consignment }: { consignment: MappedConsignment }): renders an h1/strong with `consignment.plainStatus`, a paragraph with `consignment.description`, and an ETA line rendering `consignment.estimatedDelDate ?? 'Date not yet confirmed'` (PITFALLS.md Pitfall 3 — never fabricate). Format a non-null date with date-fns `format(parseISO(estimatedDelDate), 'EEEE d MMMM')` (date-fns is already a dependency); wrap in try/catch and fall back to the raw string if parse fails. The status line uses `text-zinc-900 text-xl font-semibold`; keep it compact so it fits above the fold at 375px.
    MilestoneStepper({ currentStage }: { currentStage: MilestoneStage }): exactly the PATTERNS.md horizontal 5-step `<ol>` pattern. `const activeIdx = MILESTONE_ORDER.indexOf(currentStage)`. Steps with `idx <= activeIdx` get `bg-accent text-white`; later steps `bg-zinc-200 text-zinc-400`. Active step label `text-accent font-semibold`. Labels map: booked→'Booked', at_hub→'At hub', in_transit→'On its way', out_for_delivery→'Out for delivery', delivered→'Delivered'. `aria-label="Delivery progress"`. Each `<li>` is `flex-1` with `text-[10px]` labels so 5 steps fit 375px.
    TimeWindow({ start, end }: { start: string; end: string }): renders prominently (e.g. `bg-accent/10 text-accent rounded-lg px-4 py-3 font-medium`) the text `Arriving between {start} and {end}` (D-08). Render nothing extra — caller (TrackingResult) decides when to show it.
    MilestoneStepper.test.tsx (jsdom): `// @vitest-environment jsdom`; render with currentStage='in_transit'; assert the 'On its way' label is present and that steps 1-3 are marked done (assert the rendered text/✓ count or an accent class on the first three) and step 5 ('Delivered') is not in the done state. Assert all 5 labels render.
  </action>
  <acceptance_criteria>
    - `grep "MILESTONE_ORDER.indexOf" src/components/MilestoneStepper.tsx` matches
    - `grep "Date not yet confirmed" src/components/StatusHeader.tsx` matches (PITFALLS.md Pitfall 3)
    - `grep -E "bg-accent|text-accent" src/components/MilestoneStepper.tsx` matches and `grep -c "2563eb\|#" src/components/MilestoneStepper.tsx` finds no raw accent hex
    - `grep "Arriving between" src/components/TimeWindow.tsx` matches
    - `grep "@vitest-environment jsdom" src/components/MilestoneStepper.test.tsx` matches
    - `npx vitest run src/components/MilestoneStepper.test.tsx` passes
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "$PWD" && npx tsc --noEmit && npx vitest run src/components/MilestoneStepper.test.tsx</automated>
  </verify>
  <done>Status header (with safe null-ETA text), 5-step accent stepper (tested under jsdom), and prominent time-window component exist; no raw hex; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Below-the-fold history + vehicle (EventHistory, VehicleDetails) — PORT-04, PORT-07, D-09</name>
  <read_first>
    - .planning/phases/02-tracking-portal/02-PATTERNS.md sections "EventHistory.tsx" and "VehicleDetails.tsx"
    - src/types/consignment.ts (NexusRouteDetail)
  </read_first>
  <files>src/components/EventHistory.tsx, src/components/VehicleDetails.tsx</files>
  <action>
    EventHistory({ routeDetails }: { routeDetails: NexusRouteDetail[] }): if `routeDetails.length === 0` return null. Render reverse-chronological order — `const sorted = [...routeDetails].reverse()` (PORT-04; do not mutate the prop). `<section aria-label="Scan history">` with an `<ol>` of `<li>` each showing `<time>{event.routeDate}</time>` and `{event.status} — {event.type}` per the PATTERNS.md pattern. Below-the-fold styling (`text-sm`, `text-zinc-700/900`).
    VehicleDetails({ routeDetails }: { routeDetails: NexusRouteDetail[] }): find the delivery route detail (`routeDetails.find(r => r.type === 'Delivery')`); if none, return null (PORT-07 "when available"). When present, render `<section aria-label="Vehicle details">` showing `Registration: <span className="font-mono">{reg}</span>` and `Route status: {status}`. Never expose any other field.
    Both are Server Components, named exports, no client directives.
  </action>
  <acceptance_criteria>
    - `grep "\\[...routeDetails\\].reverse()" src/components/EventHistory.tsx` matches (reverse-chronological, non-mutating)
    - `grep "aria-label=\"Scan history\"" src/components/EventHistory.tsx` matches
    - `grep "type === 'Delivery'" src/components/VehicleDetails.tsx` matches (conditional render)
    - `grep "regNo" src/components/VehicleDetails.tsx` matches
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "$PWD" && npx tsc --noEmit</automated>
  </verify>
  <done>Reverse-chronological scan history and conditional vehicle-detail components exist, both returning null when empty/absent; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Distinct error states + chooser (ErrorState) and result composition (TrackingResult) — PORT-05, PORT-07, D-10, D-11</name>
  <read_first>
    - .planning/phases/02-tracking-portal/02-PATTERNS.md sections "ErrorState.tsx" and "TrackingResult.tsx"
    - src/types/tracking.ts (LookupFailureReason, MatchCandidate, MappedConsignment)
    - src/lib/env.ts (CONTACT_PHONE)
  </read_first>
  <files>src/components/ErrorState.tsx, src/components/ErrorState.test.tsx, src/components/TrackingResult.tsx</files>
  <behavior>
    ErrorState DOM tests (jsdom):
    - reason='not_found': renders the not_found heading text AND a tel: link containing env.CONTACT_PHONE AND a 'Try again' link to '/'
    - reason='postcode_mismatch': renders the postcode_mismatch heading and NO 'Call us' link (only Try again)
    - reason='api_error': renders its heading AND a Call us tel: link
    - reason='multiple_matches' with two candidates: renders both candidates' consignment numbers and delivery towns and plain statuses; rendered output contains NO postcode string
  </behavior>
  <action>
    ErrorState: client component (`'use client'`) because the chooser needs an onSelect handler. Props: `{ reason: LookupFailureReason; candidates?: MatchCandidate[]; onSelect?: (consignmentNumber: string) => void }`. Define `const MESSAGES: Record<LookupFailureReason, { heading: string; body: string }>` with one entry per reason exactly as in PATTERNS.md (TypeScript exhaustiveness — adding a new reason without a message must fail to compile). `const showCallUs = reason === 'not_found' || reason === 'api_error'`. When showCallUs, render `<a href={\`tel:${env.CONTACT_PHONE}\`}>Call us on {env.CONTACT_PHONE}</a>` with `text-accent`. Always render a `<a href="/">Try again</a>`. When reason==='multiple_matches' && candidates, render the chooser `<ul>` of buttons (PATTERNS.md) showing consignmentNumber + delAddressTown + plainStatus, calling `onSelect?.(c.consignmentNumber)`; render NO postcode. `role="alert"` on the wrapper.
    Note: env.CONTACT_PHONE is read server-side; since ErrorState is a client component, pass CONTACT_PHONE in as a prop OR read it in the server parent and inject. Simplest: accept an optional `contactPhone?: string` prop defaulting from a small server wrapper — BUT to keep the env single-source-of-truth, instead read CONTACT_PHONE in the server-side TrackingResult/page and pass it down. Implement ErrorState to take `contactPhone: string` as a prop (no direct env import in the client component), and have the server caller supply `env.CONTACT_PHONE`. Update the DOM test to pass a known contactPhone and assert it appears in the tel: link.
    TrackingResult: Server Component, props `{ consignment: MappedConsignment; readOnly?: boolean }`. Compose, in order: StatusHeader, MilestoneStepper, then `{consignment.currentStage === 'out_for_delivery' && consignment.startWindow && consignment.endWindow && <TimeWindow start={consignment.startWindow} end={consignment.endWindow} />}` (PORT-03), then EventHistory, then VehicleDetails. Wrap in `<div className="flex flex-col gap-6 w-full max-w-md">`. Leave a clearly-commented slot `{/* ShareBar wired in Plan 04 — hidden when readOnly */}` — do NOT implement the share button here (Plan 04 owns it). Do not import the server action.
    ErrorState.test.tsx: `// @vitest-environment jsdom`; cover the four behaviour bullets above; for multiple_matches pass two MatchCandidate objects and assert no postcode text leaks.
  </action>
  <acceptance_criteria>
    - `grep "Record<LookupFailureReason" src/components/ErrorState.tsx` matches (exhaustive map)
    - `grep "tel:" src/components/ErrorState.tsx` matches and the Call us branch is gated on `not_found`/`api_error` (`grep "showCallUs" src/components/ErrorState.tsx` matches)
    - `grep "Try again" src/components/ErrorState.tsx` matches
    - `grep "out_for_delivery" src/components/TrackingResult.tsx` matches (time-window gate)
    - `grep -c "postcode" src/components/ErrorState.tsx` == 0 (chooser shows no postcode — D-10)
    - `grep "@vitest-environment jsdom" src/components/ErrorState.test.tsx` matches
    - `npx vitest run src/components/ErrorState.test.tsx` passes all four cases
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "$PWD" && npx tsc --noEmit && npx vitest run src/components/ErrorState.test.tsx</automated>
  </verify>
  <done>ErrorState renders a distinct message per reason with exhaustive typing, Call us only on not_found/api_error, a safe candidate chooser with no postcode, and Try again always; TrackingResult composes the result with the out-for-delivery time-window gate; both tested under jsdom; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service result -> rendered DOM | Mapped consignment / candidate data is rendered to the customer's browser here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | ErrorState multiple_matches chooser | mitigate | Chooser renders only consignmentNumber + delivery town + plain status (MatchCandidate has no postcode/address-line field); test asserts no 'postcode' string in source and no postcode in rendered output (D-10) |
| T-02-10 | Information Disclosure | VehicleDetails | mitigate | Renders only regNo + route status from the delivery route detail; no driver name/phone or address fields surfaced (PORT-07) |
| T-02-11 | Tampering | status/ETA fabrication | mitigate | StatusHeader renders `estimatedDelDate ?? 'Date not yet confirmed'`; never synthesises a date; status/description come verbatim from MappedConsignment (PITFALLS.md Pitfall 3) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/components` — MilestoneStepper + ErrorState DOM tests pass
- No raw accent hex in any component (`grep -rl "2563eb" src/components` returns nothing)
- ErrorState source contains no `postcode` reference
</verification>

<success_criteria>
Every PORT-02/03/04/05/07 visible behaviour is a concrete, type-driven, individually testable component: status+ETA+stepper above the fold, prominent out-for-delivery time window, reverse-chron history, conditional vehicle detail, and one distinct helpful message per error reason with a safe no-postcode chooser and a Call us/Try again dead-end escape.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tracking-portal/02-03-SUMMARY.md`.
</output>
