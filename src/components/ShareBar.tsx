'use client';

interface Props {
  consignmentNumber: string;
}

/**
 * ShareBar — stub placeholder for Task 1 compilation.
 * Full implementation (makeShareUrl, copy link, print) wired in Task 2.
 */
export function ShareBar({ consignmentNumber: _ }: Props) {
  return null;
}
